\documentclass{ximera}


% MATH -----------------------------------------------------------
\newcommand{\nats}{\mathbb N}
\newcommand{\ints}{\mathbb Z}
\newcommand{\rats}{\mathbb Q}
\newcommand{\reals}{\mathbb R}
\newcommand{\complex}{\mathbb C}
\newcommand{\powerset}{\mathscr P}

%-------------------------------------------------------------

\pgfplotsset{compat=1.18}
\begin{document}
\begin{abstract}
 \end{abstract}

    \title{Class Discussion Activity 2}
    
    \maketitle

\section*{Exercises}

\begin{enumerate}[label=(\arabic*)]

\item Let $a$ be a constant.  Suppose that $y=\dfrac{5}{7-2x}$ is a solution to $y\,^{\prime}=ay^2$.  Find $a$ to the nearest tenth.

% y'=0.4y^2

% 0.4


\item Suppose $y(t)$ satisfies the IVP $y\,^{\prime}=y-y^2$, $y(0)=-1$.  Find $y(0.5)$ to the nearest hundredth.

% 1(y(1-y)) y' =1 ... [1/y+1/(1-y)] y'=1 ... ln|y/(1-y)|=t+C ... 1/y-1=Ce^(-t) ... 1/y=1+Ce^(-t) ... y=1/(1+Ce^{-t})

% C=-2.  y=1/(1-2e^(-t)).   Vert. asymptote at t=ln(2), so 0.5 within domain!

% y(0.5)=1/(1-2/sqrt(e))\approx -4.69

% SANITY CHECK: If y=1/(1-2e^(-t)) then y'=-2e^(-t)/(1-2e^(-t))^2 while y-y^2=(1-2e^(-t))/(1-2e^(-t))^2-1/(1-2e^(-t))^2  ... Checks out!



\item Which first order differential equation(s) are separable?  Select all that apply.

\begin{enumerate}[label=(\alph*)]
\item $y^{\,\prime}=1-y^2$ % ***
\item $y^{\,\prime}=\dfrac{x}{y}+x$ % ***
\item $y^{\,\prime}=\dfrac{y}{x}+x$  
\item $y^{\,\prime}=\tan^{-1}(xy)$ 
\item $y^{\,\prime}=e^{x^2-y}$ % ***
\item $y^{\,\prime}=y^2+xy^2$ % ***
\item $y^{\,\prime}=y^3+xy^2$ 
\item $y^{\,\prime}=\ln \left(x^y\right)$ % ***
\end{enumerate}

% (a), (b), (e), (f), (h) with options through (h)


\item The differential equation $y^{\,\prime}=2x(y^2-4)\ln (y)$ is separable. How many constant solutions of the form $y=C$ does this differential equation have? 

% 2

\item The IVP $y'=\dfrac{y^3}{2x}$, $y(1)=1$ has a unique solution $y(x)$ by the Existence and Uniqueness Theorem. Determine the width of the widest interval on which that unique solution $y(x)$ is a solution to this IVP.  If it is infinite, enter 0.  Otherwise round to the nearest hundredth. 

% e\approx 2.72

\newpage


\item  Suppose $y(t)$ is a positive-valued function satisfying $y\,^{\prime}=ty(4-y)/(1+t^2)$.  Determine $\displaystyle \lim_{t\rightarrow \infty}y(t)$.  Round to the nearest tenth. 

    % 4


\item Which the following IVP's are guaranteed to have a unique solution (on some open interval) by the Existence and Uniquesss Theorem?  Select all that apply.


\begin{enumerate}[label=(\alph*)]
\item $y\,^{\prime}=5y^{4/5}$, $y(0)=1$    % ***
\item $y\,^{\prime}=5y^{4/5}$, $y(0)=0$  
\item $\displaystyle y\,^{\prime}=\frac{x}{x-y}$, $y(1)=1$
\item $\displaystyle y\,^{\prime}=\frac{x}{x-y}$, $y(-1)=1$  % ***
\item $\displaystyle y\,^{\prime}=\left(x+y^2\right)^{1/3}$, $y(0)=0$
\item $\displaystyle y\,^{\prime}=\left(x+y^2\right)^{4/3}$, $y(0)=0$  % ***
\item $\displaystyle y\,^{\prime}=\frac{x+1}{xy}$, $y(1)=-1$  % ***
\item $\displaystyle y\,^{\prime}=\frac{\sqrt{1+y}}{1+x^2}$, $y(0)=-1$
\end{enumerate}


% (a), (d), (f) and (g) with options thru (h)

\item  Which of the following are solutions to $y'=5y^{4/5}$, $y(0)=0$?  Select all that apply.

% y^(-4/5) y'=5 ---->  y^(1/5)=t+C, C=0.


\begin{enumerate}[label=(\alph*)]
\item $y=0$ % ***
\item $y=t$
\item $y=t^2$
\item $y=t^3$
\item $y=t^4$
\item $y=t^5$ % ***
\item $y=-t$
\item $y=-t^2$
\item $y=-t^3$
\item $y=-t^4$
\item $y=-t^5$ 
\end{enumerate}


% (a) and (f) with options thru (k)

\newpage


\item Let $r$ be a real number.  For which values of $r$ below does IVP $y^{\,\prime}=xy^r$, $y(1)=0$ have more than one solution?  Select all that apply.

\begin{enumerate}[label=(\alph*)]
\item $r=-1/3$
\item $r=0$
\item $r=1/3$ % ***
\item $r=2/3$ % ***
\item $r=3/2$
\item $r=1$
\end{enumerate}

% (c) and (d) with options thru (f)

\item Suppose $y(x)$ satisfies $\displaystyle y^{\,\prime}=xy-y\ln{\left(y^x\right)}$, $y(1)=e$.  Find $y(2026)$ to the nearest hundredth.

% e \approx 2.72



\end{enumerate}

\end{document}
